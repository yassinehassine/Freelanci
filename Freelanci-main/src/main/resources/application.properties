# Application Name
spring.application.name=Freelanci

# --- DataSource Configuration (MySQL) ---
spring.datasource.url=jdbc:mysql://localhost:3306/Freelanci?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=
# Added missing property: specify the MySQL driver instead of H2's driver
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver

# --- JPA / Hibernate Configuration ---
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
# Added missing property: use a MySQL-specific dialect (adjust according to your MySQL version)
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# --- H2 Console (Optional) ---
# This property is carried over from your H2 configuration.
# It?s safe to include if you want to keep it available (e.g., for testing with an H2 profile)
spring.h2.console.enabled=true

# --- Mail Configuration ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=cryptomonnaie95@gmail.com
spring.mail.password=ihba qeck ssvr ialx
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# --- Web and Template Configuration ---
spring.main.web-application-type=servlet
spring.thymeleaf.check-template-location=false

# --- JWT Configuration ---
jwt.secret=mysupersecretkeymysupersecretkeymysupersecretkey
jwt.expiration=300000  # Expiration in milliseconds (5 minutes)
spring.jackson.date-format=yyyy-MM-dd
spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS=false
# application.properties
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.cache=false
